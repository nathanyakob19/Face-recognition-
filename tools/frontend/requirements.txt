
```txt
Flask==2.3.2
flask-cors==4.0.0
opencv-python==4.8.1.78
face-recognition==1.3.0
numpy==1.24.4
pillow==10.1.0
requests==2.31.0
mysql-connector-python==8.1.0
gunicorn==21.2.0
```

### Notes:

* `opencv-python`: For camera and image handling.
* `face-recognition`: Face detection and matching.
* `flask-cors`: Allowing cross-origin requests for React frontend.
* `mysql-connector-python`: For database communication.
* `gunicorn`: Useful for production deployment (optional).

To install these packages, use:

```bash
pip install -r requirements.txt
```

